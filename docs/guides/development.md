# Development

This guide walks through installing pulsepump and connecting to a Pico.

## Install

```bash
uv sync
```

## Run the desktop app

```bash
uv run pulsepump
```

## Flash the firmware

See {py:mod}`pulsepump.pico_loader` for the helpers used to push firmware
onto the device.
